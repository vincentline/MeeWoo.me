# 项目：
1. 项目名称：MeeWoo

# 项目规则
1. 如需启动测试服务，请执行`python start_server.py`
2. 当用户要求提交到 GitHub 时：
  - 默认提交到 GitHub 的 main 分支
  - 先拉取修改，确保本地仓库与远程仓库同步，再提交
  - 每次提交都必须根据变更内容，填写提交信息
3. 当用户要求发布网页时：
  - 如果有未提交的变更，必须先提交到 GitHub，按上面GitHub提交流程提交
  - 如果变更都提交了，把docs文件夹里的内容提交到GitHub仓库的gh-pages分支
  - 发布时，必须填写发布信息，包括发布内容等